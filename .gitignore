# Secrets — never commit these
.env
*.token

# Per-day "already sent" marker (created at runtime; cached in CI)
.sent-marker

# Already-reported transaction keys (created at runtime; cached in CI)
.seen.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
