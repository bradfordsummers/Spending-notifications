# Secrets — never commit these
.env
*.token

# Per-day "already sent" marker (created at runtime; cached in CI)
.sent-marker

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
